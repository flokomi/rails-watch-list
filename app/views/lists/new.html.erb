<div class="container">
  <div class="margin-top">
    <h1>Create a new movies list</h1>
    <div class="form">
      <%= simple_form_for(@list) do |f| %>
        <%= f.input :name %>
        <%= f.input :photo, as: :file %>
        <%= f.submit %>
      <% end %>
    </div>
  </div>
</div>
